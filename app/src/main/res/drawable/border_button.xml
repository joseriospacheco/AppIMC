<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Ajusta el grosor y el color del borde -->
    <stroke
        android:width="2dp"
        android:color="@color/colorButtonBorder" /> <!-- Cambia este color según tus necesidades -->
    <solid android:color="@color/colorButtonBorder" />
    <!-- Ajusta el radio de las esquinas si deseas bordes redondeados -->
    <corners android:radius="8dp" />
</shape>